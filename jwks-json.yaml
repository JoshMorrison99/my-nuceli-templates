id: jwks-json

info:
  name: jwks.json File Exposed
  author: shelled
  severity: low
  tags: exposure

requests:
  - method: GET
    path:
      - "{{BaseURL}}/.well-known/jwks.json"
      - "{{BaseURL}}/jwks.json"
    matchers:
      - type: status
        status:
          - 200
